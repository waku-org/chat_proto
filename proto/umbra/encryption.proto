syntax = "proto3";

package umbra.encryption;


message EncryptedPayload {
    oneof encryption {
		NoiseKN noise_KN = 3;
    }
}

message NoiseKN {
    bytes encrypted_bytes = 1;
    bytes ephemeral_pubkey = 2;
}
