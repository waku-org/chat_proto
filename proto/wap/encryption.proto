syntax = "proto3";

package wap.encryption;


message EncryptedPayload {
    oneof encryption {
		NoiseKN noise_KN = 3;
    }
}

message NoiseKN {
    bytes encrypted_bytes = 1;
    bytes ephemeral_pubkey = 2;
}
